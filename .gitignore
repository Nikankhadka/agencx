# env / secrets
.env
.env.local
.env.*.local

# node / next
node_modules/
.next/
out/
next-env.d.ts

# python
.venv/
__pycache__/
*.pyc
.pytest_cache/
.ruff_cache/

# uploads (local knowledge files, T-007)
backend/var/

# terraform
.terraform/
*.tfstate
*.tfstate.backup
*.tfvars

# os / editor
.DS_Store

# gnhf run logs (transient; objective + handoff in .agents/ are tracked)
.agents/gnhf-logs/

# opencode session plan drafts (transient; decisions get archived to docs/)
.opencode/plans/

# lavish skill plan-approval artifacts (transient; decisions get archived to docs/)
.lavish/

# Vercel CLI link + pulled env (never committed)
.vercel
